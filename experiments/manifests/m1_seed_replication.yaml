# M1 seed replication - pre-registration (Phase 2 of the unattended batch).
# Frozen BEFORE any replicate run; the seed values and the analysis rules below
# must not change based on results (no outcome-dependent seed selection).
audit: docs/seed_control_audit.md

seeds:
  seed2: 20260918    # runs/m1_seed2, scripts/run_m1_seed2.sh
  seed3: 20260919    # runs/m1_seed3, only if seed2 completes and the host is stable

seed_knobs:
  +data.seed: "dataloader shuffle generator (main_ppo.py:368); the only configurable seed in this pinned build"
  rollout_engine_seed: "NOT configurable on the FSDP path (RolloutConfig has no seed field; vLLM engine seed fixed at 0) - independence comes from the diverging request streams, verified by the live sanity check"
  seed1_effective_defaults: {data.seed: 1}

recipe: "identical to E019/P3-B (GRPO/DrGRPO, FULL-FT, n=8, tb4, lr 2e-6, no KL, multiturn off, 4096)"

rules:
  - fresh start from Kimina-Prover-Distill-0.6B (never from theta30/theta60)
  - run the full 60 steps unless an unattended stop condition triggers
    (NaN/OOM/corruption/storage) - no stopping on low reward or low IGR
  - live sanity after step 1: statement sequence must differ from seed1
    (otherwise stop: seed plumbing invalid)
  - primary analysis of replicates is TRAINING DYNAMICS (IGR/Z/O/score by range);
    the reused P3-C development set is not used to pick a "best" seed

expected_artifacts:
  dynamics: experiments/results/e020_seed2_dynamics.json (via scripts/rollout_dynamics.py --dir)
  trainer_metrics: experiments/results/e020_seed2_trainer_metrics.json
  checkpoints: runs/m1_seed2/global_step_30, global_step_60 (export to runs/m1_seed2_models)

outcome_seed2:                 # completed 2026-09-18 01:39 UTC
  steps: "60/60, exit 0, wall 2:18:10 (~2.30 GPU-h at ~138 s/step)"
  live_sanity: "step-1 statement sequence disjoint from seed1 (overlap 0)"
  dynamics:
    1-10:  {igr: 0.150, score: 0.0437}
    11-20: {igr: 0.050, score: 0.0437}
    21-30: {igr: 0.175, score: 0.0750}
    31-40: {igr: 0.150, score: 0.0844}
    41-50: {igr: 0.250, score: 0.1437}
    51-60: {igr: 0.125, score: 0.0563}
    all:   {igr: 0.150, score: 0.0745}
  findings: >
    Overall magnitude replicates (IGR 0.150 vs seed1 0.158). The rise-to-
    informative-frontier-then-late-decline shape appears in BOTH seeds (the
    final segment is below each seed's own peak), but the peak LOCATION is
    seed-dependent (seed1 21-30, seed2 41-50): the '21-30 peak then
    regression' is not a fixed cross-seed pattern. Descriptive only - two
    training seeds cannot support seed-level significance claims.
  checkpoint_notes: >
    step60 exported bitwise-identical (rel_L2 vs base 2.41e-4, same magnitude
    as seed1's 2.35e-4). Actor weights of steps 10/20/30 were pruned by
    max_actor_ckpt_to_keep=3 (only step60 is needed by the final holdout;
    dynamics come from rollout dumps and are unaffected).

seed3_decision_r1_r3: aborted-deferred   # 历史记录（2026-09-18）
seed3_decision: completed (E022-r4, 2026-09-18; see outcome_seed3_r4)
seed3_outcome_r1: >
  Launched 2026-09-18 04:42 UTC (supervisor m1s3sup); steps 1-3 completed normally
  (dumps 1-3.jsonl present; live sanity confirmed pairwise-disjoint step-1 statements
  across all three seeds) and then the unit was systemd-oomd killed at 05:02:32 UTC.
  Root cause: a NON-project GPU training job (experiment/train_p2_calibration.py,
  run fullsa_p2_gap_calib_640_100, login session 628, ~5.8 GB VRAM, started ~04:53 UTC)
  shared the GPU, breaking the single-GPU-job premise and adding memory pressure.
  Not touched/stopped by this project. Per protocol (no infinite retries after the
  same-cause failure repeats; one GPU-heavy job at a time) seed3 is deferred, and the
  GPU-dependent remainder (final holdout evaluation, MiniF2F) is paused until the
  external job finishes and the host is stable. Partial state kept at runs/m1_seed3/.
resume_conditions: >
  When the host is idle again: relaunch seed3 with the SAME pre-registered seed
  (20260919), then build the final holdout (scripts/build_final_holdout.py) and run the
  theta0/seed1/seed2/seed3 holdout evaluations, then MiniF2F.
  SATISFIED via E022-r4 (2026-09-18 16:41-19:19 UTC; single attempt, exit 0); the holdout build is the next step.
retry_history_r2_r3: >
  2026-09-18: relaunch r2 (in place, same seed) was oomd-killed at 14:39:02 UTC
  (user-slice pressure 89%); r3 used a two-unit topology (tiny supervisor + separate
  training unit) with <=3 bounded attempts - attempt 1 reached step 3 then was killed
  at 15:35:38 (pressure 75.8%), attempts 2/3 died during model loading. Bounded retries
  exhausted; seed3 is NOT completable in this window under the current shared-host
  memory pressure (no external GPU job present at the time; other agents/IDE load
  observed). Prerequisite to resume (either): user runs
  'sudo systemctl stop systemd-oomd systemd-oomd.socket', or a verified quiet window.
outcome_seed3_r4:              # completed 2026-09-18 16:41:46-19:18:59 UTC (wall 2:37:13)
  preconditions: "user-approved stop of systemd-oomd (service+socket) in a quiet window; Lean server REPL pool reset (docker restart + prewarm)"
  steps: "60/60, exit 0, single attempt (no retry), ~157 s/step; checkpoints 10-60 present"
  live_sanity: "step-1 statements disjoint from seed1; identical to r2/r3 step-1 (same pre-registered seed 20260919 - determinism re-confirmed)"
  dynamics:
    1-10:  {igr: 0.250, score: 0.1000}
    11-20: {igr: 0.125, score: 0.0594}
    21-30: {igr: 0.125, score: 0.0938}
    31-40: {igr: 0.100, score: 0.0531}
    41-50: {igr: 0.150, score: 0.0844}
    51-60: {igr: 0.150, score: 0.0656}
    all:   {igr: 0.150, score: 0.0760}
  checkpoint: "runs/m1_seed3_models/step_60 (311/311 keys, identical=True, 3.01 GB; rel_L2 vs base 2.17e-4)"
  findings: >
    Third independent trajectory completes the pre-registered replication set:
    all-period IGR 0.158 / 0.150 / 0.150 (seed1/2/3) - magnitude replicates.
    Peak location is again seed-dependent (21-30 / 41-50 / 1-10); all three
    seeds end below their own peak. Descriptive only - three training seeds.
  outcome: "holdout seal prerequisites satisfied; holdout sealed 2026-09-18 (experiments/manifests/m1_final_holdout.json: excluded 763 / eligible 6857 / selected 128)."
fallback: >
  NOT NEEDED (seed3 completed as E022-r4). Historical rule: Protocol allows sealing the 128-theorem final holdout WITHOUT seed3 (it is optional)
  and evaluating theta0/seed1/seed2 (+seed3 if it ever completes). The holdout builder
  was fixed/verified (excluded 595 / eligible 7025 / selected 128) and must be sealed
  only after the training round's final decision on seed3.
